# Copie este arquivo para ".env.local" (que é ignorado pelo git) e
# preencha com sua chave real para testar localmente com "vercel dev".
#
# Em produção, configure a mesma variável em:
# Vercel -> seu projeto -> Settings -> Environment Variables
#
# NUNCA coloque a chave real neste arquivo nem em nenhum arquivo commitado.
OPENAI_API_KEY=sk-...
